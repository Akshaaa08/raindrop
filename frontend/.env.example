# Copy this to .env.local for local dev (no changes needed — Vite proxy handles /api)
# In Vercel dashboard, set this to your Render backend URL:
VITE_API_URL=https://your-backend-name.onrender.com/api
